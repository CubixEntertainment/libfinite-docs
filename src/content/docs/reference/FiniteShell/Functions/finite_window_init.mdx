---
title: finite_window_init
description: The finite_window_init function.
sidebar:
    order: 2
---

import { Aside } from '@astrojs/starlight/components';

<Aside> finite_window_init was last modifed in **v0.6.0** </Aside>

The `finite_window_init` function creates a new window with a given [`FiniteShell`](/reference/finiteshell/finiteshell/).

```c
void finite_window_init(FiniteShell *shell)
```

## Parameters

| Type                           | Description                                                                            |
| ------------------------------ | -------------------------------------------------------------------------------------- |
|`FiniteShell *shell`|The FiniteShell to create a window for.|

## Code Example

```c
finite_window_init(myShell);

if (!myShell) {
    FINITE_LOG_FATAL("Unable to make shell");
}
```

## Standard Usage

When creating a new application (NOT A POPUP), you **must** call this function. Although libfinite support custom implementations of some function, all libfinite functions that interact with the window expect the data provided by this function.
