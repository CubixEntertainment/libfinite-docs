---
title: finite_draw_create_snapshot
description: The finite_draw_create_snapshot function.
---

import { Aside } from "@astrojs/starlight/components";

<Aside> finite_draw_create_snapshot was last modifed in **v0.6.0** </Aside>

The `finite_draw_create_snapshot` function attempts to create a restore point of a cairo surface.

```c
void finite_draw_create_snapshot(FiniteShell *shell)
```

## Parameters

| Type                 | Description                                                                |
| -------------------- | -------------------------------------------------------------------------- |
| `FiniteShell *shell` | The [`FiniteShell`](/reference/finiteshell/finiteshell/) where the window is. |

## Code Example

```c
#include <finite/draw.h>

finite_shm_alloc(shell, true);
finite_draw_create_snapshot(myShell);
```

## Standard Usage

<Aside type="caution">
  finite_draw_create_snapshot was deprecated in **v0.7.0** and will be removed in
  v1.0.0. Use `cairo_save` instead.
</Aside>

This function must have a valid `FiniteShell`

This function uses Cairo which requires a valid cairo_surface_t in `FiniteShell.cairo_surface` beforehand.

This function copys all current data on screen and stores it in `FiniteShell.snapshot` in `FiniteShell`

At some point [`finite_draw_finish`](/reference/finitedraw/functions/finite_draw_finish/) should be called to apply the changes made by the snapshot visually.
