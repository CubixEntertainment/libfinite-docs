---
title: finite_gamepad_init
description: The finite_gamepad_init function
sidebar:
  order: 1
---

import { Aside } from "@astrojs/starlight/components";

<Aside> finite_gamepad_joystick_get_value was added in **v0.7.0** </Aside>

The `finite_gamepad_init` function creates a [`FiniteGamepad`](/reference/finiteinput/finitegamepad/types/finitegamepad/)

```c
bool finite_gamepad_init(FiniteShell *shell)
```

## Properties

| Type                 | Description                                                                           |
| -------------------- | ------------------------------------------------------------------------------------- |
| `FiniteShell *shell` | The [`FiniteShell`](/reference/finiteshell/finiteshell/) the gamepad is connected to. |

## Code Example

```c
bool withGP = finite_gamepad_init(myShell);
myShell->canHomeMenu = false;

if (!withGP) {
    FINITE_LOG_ERROR("Can't poll controller data");
}
```

## Specification

`finite_gamepad_init` returns false only if the program is unable to search for gamepads. If no gamepads are connected, `FiniteShell.gamepadAvailable` will be false. Developers should verify a controller is available before doing anything controller intensive.
