---
title: finite_key_from_string
description: The finite_key_from_string function.
sidebar:
  order: 5
---

import { Aside } from "@astrojs/starlight/components";

<Aside> finite_key_from_string was last modifed in **v0.6.0** </Aside>

The `finite_key_from_string` function attempt to convert a string to a [`FiniteKey`](/reference/finiteinput/finitekeyboard/enums/finitekey)

```c
FiniteKey finite_key_from_string(const char *name)
```

## Parameters

| Type            | Description             |
| --------------- | ----------------------- |
| `FiniteKey key` | The FiniteKey to lookup |

```c
#include <finite/draw.h>
#include <finite/input.h>

FiniteShell *myShell = finite_shell_init("wayland-0");

if (!myShell) {
    FINITE_LOG_FATAL("Unable to init shell");
}

FiniteKeyboard *kbd = finite_input_keyboard_init(myShell->display);

while (wl_display_dispatch(myShell->display) != -1) {
    if (!finite_key_valid(FINITE_KEY_A)) {
        FINITE_LOG_ERROR("Finite Key A is not valid");
    }

    if (finite_key_pressed(finite_key_from_string("A"), kbd)) {
        FINITE_LOG("A was pressed");
    }
}
```

## Standard Usage

To convert a `FiniteKey` to a string, use [`finite_key_string_from_key`](/reference/finiteinput/finitekeyboard/functions/finite_key_string_from_key/)
